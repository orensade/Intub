[project]
name = "intub-backend"
version = "0.1.0"
description = "Backend API for Intubation Difficulty Assessment"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
    "flask-cors>=4.0.0",
]
